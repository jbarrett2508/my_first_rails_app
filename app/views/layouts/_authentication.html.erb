<% if current_user %>
  <h2>Signed in as <%= current_user.username %> | <%= link_to 'Logout', logout_path, method: :delete %></h2>
<% else %>
  <h2><%= link_to 'Login', login_path %> | <%= link_to 'Sign Up', new_user_path %></h2>
<% end %>

